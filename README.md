# alu-web_react
React Module - TypeScript Project

## Overview
This is a TypeScript project setup ready for React development. The project includes all necessary configurations and example code to get you started immediately.

## Project Structure
```
alu-web_react/
├── src/                    # Source files
│   ├── index.ts           # Main entry point
│   └── utils.ts           # Utility functions and classes
├── dist/                   # Compiled JavaScript output (generated)
├── tsconfig.json          # TypeScript configuration
├── package.json           # Project dependencies and scripts
├── .eslintrc.json         # ESLint configuration
└── .gitignore             # Git ignore rules
```

## Getting Started

### Prerequisites
- Node.js (v16 or higher)
- npm or yarn

### Installation
```bash
npm install
```

### Available Scripts

#### Build the project
Compile TypeScript to JavaScript:
```bash
npm run build
```

#### Run the project
Execute the compiled JavaScript:
```bash
npm start
```

#### Development mode
Watch for changes and recompile automatically:
```bash
npm run dev
```

#### Lint the code
Check for code quality issues:
```bash
npm run lint
```

#### Clean build artifacts
Remove the dist directory:
```bash
npm run clean
```

## TypeScript Configuration

The project is configured with strict TypeScript settings:
- Target: ES2020
- Strict type checking enabled
- Source maps for debugging
- Declaration files generated

## Features

- ✅ TypeScript 5.3+
- ✅ ESLint configuration
- ✅ Strict type checking
- ✅ Example code with classes and interfaces
- ✅ Build and development scripts
- ✅ Source maps for debugging

## Example Code

The project includes example TypeScript code:
- `Greeter` class demonstrating class usage
- `add` function showing type-safe functions
- `User` interface and `formatUser` function demonstrating interfaces

## Next Steps

1. Install dependencies: `npm install`
2. Build the project: `npm run build`
3. Run the project: `npm start`
4. Start developing your own TypeScript code in the `src/` directory

## License
ISC
